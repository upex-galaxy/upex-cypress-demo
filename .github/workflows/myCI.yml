name: Blackhole CI Pipeline
run-name: ${{github.actor}} ran a Sanity Test on ${{github.ref_name}}

on:
    push:
        branches-ignore:
            - main
            - QA

jobs:
    SanityTest:
        runs-on: ubuntu-latest

        steps:
            - name: 📦Set up Node.js
              uses: actions/setup-node@v2
              with:
                  node-version: 18

            - run: mkdir blackhole

            - run: cd blackhole

            - run: touch galaxy.js

            - run: echo "console.log('hola mama estoy en el CI');" >> galaxy.js

            - run: node galaxy.js
