describe('ToolsQA | Elements | Buttons', () => {
	it('1137 | TC01: Validar funcionamiento del botón "Double Click Me"', () => {
		expect(1).to.equal(1);
	});
	it('1137 | TC02: Validar funcionamiento del botón "Rigth Click Me"', () => {
		expect(1).to.equal(1);
	});
	it('nuevo test probando git reset --hard', () => {
		cy.log('Ya cortamos, tengo hambre');
	});
});
