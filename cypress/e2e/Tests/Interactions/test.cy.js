describe('Search Gaza war in google and verify if you get the properly result', () => {
	beforeEach('PRC: User must be at Google front page', () => {
		cy.visit('https://www.google.com/');
	});
	it('Tc1 validate search to be successful', () => {
		cy.get();
	});
});
